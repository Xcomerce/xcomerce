/** Temporarily hide billing menu and page until the client enables subscriptions. */
export const BILLING_PAGE_ENABLED = false

/** Auto-proposta desativada até planos pagos (reunião 03/08/2026). */
export const AUTO_OFFER_ENABLED = false

/** Limite de itens no catálogo por plano — desativado na fase de testes com o cliente. */
export const CATALOG_LIMITS_ENABLED = false
